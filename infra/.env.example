# Copy to infra/.env on the VPS; chmod 600 infra/.env.
DOMAIN=clima.seudominio.com.br
WU_API_KEY=
WU_STATION_ID=INOVAF30
WU_PUBLIC_FALLBACK=true
WU_PAGE_URL=https://www.wunderground.com/dashboard/pws/INOVAF30
YOUTUBE_VIDEO_ID=
# Optional browser-compatible public stream (HLS .m3u8 or MP4), for direct site playback.
CAMERA_STREAM_URL=
# Reachable from VPS through router VPN or DDNS + restricted port forwarding.
# Percent-encode special characters in camera username/password.
CAMERA_RTSP_URL=rtsp://leitura:SENHA@HOST_DDNS_OU_IP_VPN:554/Preview_01_main
# Copy full RTMPS URL and secret key from YouTube Studio. Never put in frontend.
YOUTUBE_RTMPS_URL=rtmps://a.rtmps.youtube.com:443/live2/CHAVE_SECRETA
